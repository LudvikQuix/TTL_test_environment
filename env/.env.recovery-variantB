# State-recovery test, Variant B — fail-fast: expect StateRecoveryOffsetOutOfRange crash.
# Set these on the Recovery Filter deployment (see dev-planning/state-recovery-test/runbook.md).
AUTO_RECOVER=false
OFFSET_RESET=earliest
