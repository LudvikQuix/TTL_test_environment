# State-recovery test, Variant C — auto-recover, skip backlog (resume at high watermark).
# Set these on the Recovery Filter deployment (see dev-planning/state-recovery-test/runbook.md).
AUTO_RECOVER=true
OFFSET_RESET=latest
