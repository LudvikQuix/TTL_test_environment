name: Dedup Filter No TTL
language: python
variables:
  - name: input
    inputType: InputTopic
    description: Topic with raw events (shared input across all dedup filters)
    defaultValue: raw-events
    required: true
  - name: output
    inputType: OutputTopic
    description: Topic with deduplicated events (no-TTL pipeline)
    defaultValue: deduped-events-no-ttl
    required: true
  - name: VALUE_PADDING_BYTES
    inputType: FreeText
    description: Pad each stored value with this many bytes (matches the TTL filter for apples-to-apples comparison).
    defaultValue: "200"
    required: false
  - name: LOGGER
    inputType: FreeText
    description: "on/off. off disables the periodic status logger (skips its per-interval rocksdb key scan). Set off in production for performance."
    defaultValue: "on"
    required: false
dockerfile: build/dockerfile
runEntryPoint: main.py
defaultFile: main.py
