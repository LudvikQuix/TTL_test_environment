Quix__Sdk__Token=

# Input/output topics
input=ttl-verify-input
output=ttl-verify-feature-out

# TTL must be SHORTER than the generator's WAIT_SECONDS so batch 2 sees expired state
STATE_TTL_SECONDS=5
